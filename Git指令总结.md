git add 暂存修改后的文件，准备将其包含再下一次提交中
git commint -m:-m用于添加提交说明，提交暂存区的文件到版本历史纪录，并附带一条描述性消息，同时提交目录结构，生成快照（就是保存当前的目录结构，以及每个文件对应的二进制对象）
git push 将提交从本地存储库上传到远程存储库
git branch 在存储库中创建或列出分支，允许独立处理不同的功能
git status 显示项目文件的当前状态，指示哪些文件已被修改、暂存或未跟踪
git diff 显示文件、分支或提交之间的差异
git rebase 把一个分支的更待重新应用到另一个分支上，从而保持提交历史的整洁
git init 在项目文件夹中创建一个新的Git存储库，并对其进行设置用于版本控制
git stash 临时保存更改，以便可以处理其他事情，然后再准备好后将其带回
git push 将提交从本地存储库上传到远程存储库（如GitHub）
git log 显示提交的历史记录，使可以看到随时间的变化
git rm 从工作目录中删除文件并在下次提交时进行删除
git fetch从远程存储库下载更新么人无需将其合并到本地分支
git merge 将一个分支的更改合并到另一个分支中，有助于集成新功能或修复错误
git pull 从远程存储库下载更新并将其合并到本地存储库中
git clone 将现有的Git存储库从远程服务器复制到本地计算机
git reset通过将当前分支移动到以前的提交、取消暂存文件或丢弃提交来撤销更改
git tag 标记项目历史中的特定点，通常用于表示版本或发布
git checkout在分支间切换或恢复工作树文件
